import vertex from"./basicVertex";import{funcBlurDirection}from"../utils/blur";const fragment=`\nprecision mediump float;\nvarying vec2 fragTexture;\nuniform sampler2D textureMap;\nuniform float size;\nuniform float direction;\nuniform vec2 resolution;\n\n${funcBlurDirection}\n\nvoid main() {\n  // vec3 blur = funcBlurDirection(textureMap, fragTexture, resolution, direction, size).xyz;\n  // float alpha = texture2D(textureMap, fragTexture).a;\n  // gl_FragColor = vec4(blur, alpha);\n  gl_FragColor = funcBlurDirection(textureMap, fragTexture, resolution, direction, size);\n}\n`;export default{vertex,fragment,attributes:["position","texture"],uniforms:["flipY","textureMap","resolution","size","direction"]};