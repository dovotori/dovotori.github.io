// import { mapFromRange } from './numbers';

// export const createLinePoints = (count) => {
//   const points = new Array(count);
//   points.fill([0,0,0]);
//   return points.reduce((acc, cur, index) => {
//     const point = [
//       mapFromRange(index, 0, count, -0.5, 0.5),
//       cur[1],
//       cur[2]
//     ];
//     return [ ...acc, ...point ];
//   }, []);
// }
